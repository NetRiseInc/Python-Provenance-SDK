"""
This file was auto-generated by openapi-python-client.
Do not modify this file directly - changes will be overwritten.
"""

from http import HTTPStatus
from typing import Any

import httpx

from ... import errors
from ...client import AuthenticatedClient, Client
from ...models.advisory_response import AdvisoryResponse
from ...models.error_response import ErrorResponse
from ...types import UNSET, Response


def _get_kwargs(
    *,
    advisory_id: str,
) -> dict[str, Any]:

    params: dict[str, Any] = {}

    params["advisory_id"] = advisory_id

    params = {k: v for k, v in params.items() if v is not UNSET and v is not None}

    _kwargs: dict[str, Any] = {
        "method": "get",
        "url": "/advisory",
        "params": params,
    }

    return _kwargs


def _parse_response(
    *, client: AuthenticatedClient | Client, response: httpx.Response
) -> AdvisoryResponse | ErrorResponse | None:
    if response.status_code == 200:
        response_200 = AdvisoryResponse.from_dict(response.json())

        return response_200

    if response.status_code == 400:
        response_400 = ErrorResponse.from_dict(response.json())

        return response_400

    if response.status_code == 401:
        response_401 = ErrorResponse.from_dict(response.json())

        return response_401

    if response.status_code == 404:
        response_404 = ErrorResponse.from_dict(response.json())

        return response_404

    if response.status_code == 500:
        response_500 = ErrorResponse.from_dict(response.json())

        return response_500

    if client.raise_on_unexpected_status:
        raise errors.UnexpectedStatus(response.status_code, response.content)
    else:
        return None


def _build_response(
    *, client: AuthenticatedClient | Client, response: httpx.Response
) -> Response[AdvisoryResponse | ErrorResponse]:
    return Response(
        status_code=HTTPStatus(response.status_code),
        content=response.content,
        headers=response.headers,
        parsed=_parse_response(client=client, response=response),
    )


def sync_detailed(
    *,
    client: AuthenticatedClient,
    advisory_id: str,
) -> Response[AdvisoryResponse | ErrorResponse]:
    """Get information about an advisory

     Get information about an advisory

    Args:
        advisory_id (str):

    Raises:
        errors.UnexpectedStatus: If the server returns an undocumented status code and Client.raise_on_unexpected_status is True.
        httpx.TimeoutException: If the request takes longer than Client.timeout.

    Returns:
        Response[AdvisoryResponse | ErrorResponse]
    """

    kwargs = _get_kwargs(
        advisory_id=advisory_id,
    )

    response = client.get_httpx_client().request(
        **kwargs,
    )

    return _build_response(client=client, response=response)


def sync(
    *,
    client: AuthenticatedClient,
    advisory_id: str,
) -> AdvisoryResponse | ErrorResponse | None:
    """Get information about an advisory

     Get information about an advisory

    Args:
        advisory_id (str):

    Raises:
        errors.UnexpectedStatus: If the server returns an undocumented status code and Client.raise_on_unexpected_status is True.
        httpx.TimeoutException: If the request takes longer than Client.timeout.

    Returns:
        AdvisoryResponse | ErrorResponse
    """

    return sync_detailed(
        client=client,
        advisory_id=advisory_id,
    ).parsed


async def asyncio_detailed(
    *,
    client: AuthenticatedClient,
    advisory_id: str,
) -> Response[AdvisoryResponse | ErrorResponse]:
    """Get information about an advisory

     Get information about an advisory

    Args:
        advisory_id (str):

    Raises:
        errors.UnexpectedStatus: If the server returns an undocumented status code and Client.raise_on_unexpected_status is True.
        httpx.TimeoutException: If the request takes longer than Client.timeout.

    Returns:
        Response[AdvisoryResponse | ErrorResponse]
    """

    kwargs = _get_kwargs(
        advisory_id=advisory_id,
    )

    response = await client.get_async_httpx_client().request(**kwargs)

    return _build_response(client=client, response=response)


async def asyncio(
    *,
    client: AuthenticatedClient,
    advisory_id: str,
) -> AdvisoryResponse | ErrorResponse | None:
    """Get information about an advisory

     Get information about an advisory

    Args:
        advisory_id (str):

    Raises:
        errors.UnexpectedStatus: If the server returns an undocumented status code and Client.raise_on_unexpected_status is True.
        httpx.TimeoutException: If the request takes longer than Client.timeout.

    Returns:
        AdvisoryResponse | ErrorResponse
    """

    return (
        await asyncio_detailed(
            client=client,
            advisory_id=advisory_id,
        )
    ).parsed
